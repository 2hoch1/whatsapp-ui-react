# Tasks

1. Do not read the files contents, just run a file comparison against E:\GitHub\bpsets\next-monorepo-template.
2. This should be a monorepo repository at the end of the convertion /goal.
3. Do your research, then create a plan for each step. At the top tell me your descions and only there and then show me a step by step plan.

## Context

For Context this repository was running before on NPM with a backup branch: backup/main. The main branch was deleted completly to convert this into a next-monorepo, with a real website and downloadedable components like shadcn, with a cleaner structure with barrled components and freadlier useabliety.

## Convertion

Important: Do not delete the already existing folder and files. Edit them is allowed, moving is allowed. Ask if you have a better solution.

We wanna make the components clean and easy to use.

1. The first step is to convert this repository and make it work agian is to add all missing files from the next-monorepo-template to this repository.
2. Check if the project is executable and everything is installed at root.
3. Use the shadcn cli tool to install the already registered but empty E:\GitHub\2hoch1\whatsapp-ui-react\packages\react\src\ui\components.
4. Register all the sub packages (that doesn't mean to make the code runable) and setup the website.
5. For testing purposes on the website you can use this template for refernce E:\GitHub\bpsets\registry-template and E:\GitHub\2hoch1\shadcn-ui.
6. The last step is to import all files from the backup branch ontop of the current setup. This includes to adjust them making them work with the current setup. The code deeply nested and we gonna switch to the shadcn styled barreld version. https://ui.shadcn.com/docs/components/base/accordion, https://ui.shadcn.com/docs/components/base/card

## Bouderies

1. Launch multiple agents via superpowers, one context agent (director), one executer, one planer and so on, if you find more use them.
2. Ask for guides instead if something is not cleaer instead of interpreting the best option.
